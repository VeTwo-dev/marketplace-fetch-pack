/**
 * CLI-layer constants.
 * Re-exports from the shared constants module so the CLI doesn't own
 * the canonical definition of CLI_NAME.
 */
export { CLI_NAME, LEGACY_CLI_NAMES } from "../constants.js";
